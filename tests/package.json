{
  "name": "itapolitana-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Testes E2E Playwright — Itapolitana Cajuru",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  },
  "engines": {
    "node": ">=20"
  }
}
