{
  "name": "itapolitana-api-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker — backend seguro para PII da Sorveteria Itapolitana Cajuru",
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "tail":   "wrangler tail",
    "test":   "node --test tests/production-guard.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  },
  "engines": {
    "node": ">=18"
  }
}
